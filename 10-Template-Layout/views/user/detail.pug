extends ../layouts/master.layout.pug
block content
    h1 Detail User
    table(class="table table-striped table-bordered")
        thead(class="thead-dark")
            th Name
            th Email
            th Phone
        tbody
            td= user.name
            td= user.email
            td= user.phone 